---
description: generate and run unit tests for the Recipe Helper project
---

1. Ensure the Python environment is set up and dependencies are installed.
2. Run pytest on the `tests/` directory.
// turbo
3. `pytest tests/`
